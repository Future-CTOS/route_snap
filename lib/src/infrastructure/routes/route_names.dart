import 'route_paths.dart';

class RouteNames {
  const RouteNames._();

  // const makes cache system!
  static const String splash = RoutePaths.splash;
  static const String mapSelector = RoutePaths.mapSelector;
}